/*
#include <iostream>
using namespace std;

int main()
{
    int N, v, count = 0;
    cin >> N;
    int* array = new int[N];
    
    for(int i=0; i<N; i++)
    {
        cin >> array[i];
    }

    cin >> v;
    for(int i=0; i<N; i++)
    {
        if(v == array[i]) count++;
    }
    cout << count;

    return 0;
}
*/
