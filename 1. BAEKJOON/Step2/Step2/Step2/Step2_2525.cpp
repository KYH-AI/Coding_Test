/*#include <iostream>
using namespace std;

int main()
{
    // c_h(시)와 c_m(분)을 하나의 분으로 변환한 뒤, m를 더하고 나온 결과 값을 다시 시와 분 으로 변환하는 방법
    int c_h, c_m, m, temp_m;
    cin >> c_h >> c_m;
    cin >> m;
    
    temp_m = (c_h * 60) + c_m + m;

    c_h = (temp_m / 60) % 24;
    c_m = temp_m % 60;
    cout << c_h << ' ' << c_m;
    return 0;
}*/